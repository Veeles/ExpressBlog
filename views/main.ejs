<%- include('partials/header.ejs') %>
<div class="blog-place">
    <div class="blog-border">
        <h1 id="h1-latest">Latest Post</h1>
        <form action="/add" method="get">
            <button class="add-post" name="add" type="submit">Add Post</button>
        </form>
        
    </div>
    <div class="container">
        
        <div id="item2">
            <div class="blog-body">
                <% if (locals.posts) {
                    for(i=0;i < posts.length; i++) {%>
                
                <h3><%=posts[i]['title'] %></h3>
                <p><%= posts[i]['description'].slice(0,100) %> ...</p>
                <% } 
                }else {%>
                <h3>Oops! We don’t have any posts. You must write one! Press the button on the right!</h3>
                <% }%>
            </div>
        </div>
    </div>
    
   
</div>

<%- include('partials/footer.ejs') %>